---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: dexoauth2clients.dex.araihu.com
spec:
  group: dex.araihu.com
  names:
    kind: DexOAuth2Client
    listKind: DexOAuth2ClientList
    plural: dexoauth2clients
    singular: dexoauth2client
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DexOAuth2Client is the Schema for the dexoauth2clients API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DexOAuth2ClientSpec defines the desired state of an OAuth2
              client.
            properties:
              adoptExisting:
                default: false
                description: AdoptExisting authorizes takeover of an unowned matching
                  Dex record.
                type: boolean
              allowedConnectors:
                description: AllowedConnectors restricts connectors usable by this
                  client.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              deletionPolicy:
                default: Delete
                description: DeletionPolicy controls cleanup of Dex state and generated
                  Secrets.
                enum:
                - Delete
                - Retain
                type: string
              id:
                description: ID is the immutable Dex client identifier.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: id is immutable
                  rule: self == oldSelf
              logoURL:
                description: LogoURL is an optional client logo URL. Empty removes
                  the logo through recreate.
                type: string
              name:
                description: Name is the mutable display name.
                minLength: 1
                type: string
              public:
                description: Public selects a public OAuth2 client and is immutable.
                type: boolean
                x-kubernetes-validations:
                - message: public is immutable
                  rule: self == oldSelf
              redirectURIs:
                description: RedirectURIs are accepted OAuth2 redirect URIs.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              secret:
                description: Secret configures confidential client material and is
                  forbidden for public clients.
                properties:
                  generated:
                    description: Generated configures an operator-generated client
                      secret.
                    properties:
                      secretName:
                        description: SecretName receives the generated clientSecret
                          key.
                        minLength: 1
                        type: string
                    required:
                    - secretName
                    type: object
                  providedSecretRef:
                    description: ProvidedSecretRef selects an existing client secret.
                    properties:
                      key:
                        description: Key is the data key within the Secret.
                        minLength: 1
                        type: string
                      name:
                        description: Name is the Secret name.
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  rotationNonce:
                    description: RotationNonce authorizes delete/recreate secret rotation
                      when changed.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: exactly one of providedSecretRef or generated is required
                  rule: has(self.providedSecretRef) != has(self.generated)
              trustedPeers:
                description: TrustedPeers are clients trusted for cross-client assertions.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
            required:
            - id
            - name
            - public
            type: object
            x-kubernetes-validations:
            - message: public clients must omit secret and confidential clients must
                provide it
              rule: 'self.public ? !has(self.secret) : has(self.secret)'
          status:
            description: DexOAuth2ClientStatus defines observed non-secret state.
            properties:
              appliedSecretResourceVersion:
                description: AppliedSecretResourceVersion is the last converged input/generated
                  Secret version.
                type: string
              conditions:
                description: Conditions report Ready, Compatible, and Drifted state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              externalID:
                description: ExternalID is the Dex client ID ownership preclaim.
                type: string
              handledRotationNonce:
                description: HandledRotationNonce is the last successfully applied
                  secret rotation nonce.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent converged or evaluated
                  spec generation.
                format: int64
                type: integer
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
